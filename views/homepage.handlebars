{{!-- HOMEPAGE --}}
<ol class="post-list d-flex justify-content-center row mx-2 my-2">
    {{#each posts as |post|}}
    <li class="list-unstyled">
        {{> post-info post }}
    </li>
    {{/each}}
</ol>